#include<bits/stdc++.h>
#define ll long long
#define F first
#define S second
#define weakson ios::sync_with_stdio(0), cin.tie(0);
#define pll pair<ll, ll>
#define dbg(x) cout << #x << " = " << x << endl;
using namespace std;

int main(){
	weakson;

	int n, x;
	cin >> n >> x;

	bitset<10005> dp;

	for (int i = 0; i < n; i++){
		int a, b;
		cin >> a >> b;
		for (int j = 0; j < x; j++){
			if (j + a <
		}
	}
}
