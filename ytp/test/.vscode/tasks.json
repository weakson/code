{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "kotlin native",
            "type": "shell",
            "command": "/home/mint/download/kotlin-native-linux-x86_64-1.7.10/bin/konanc ${file} -g -o ${fileBasenameNoExtension}"
        },
        {
            "label": "g++",
            "type": "shell",
            "command": "g++ ${file} -g -o ${fileBasenameNoExtension}.exe"
        }

    ]
}