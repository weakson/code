#include<bits/stdc++.h>
#define ll long long
#define weakson ios::sync_with_stdio(0), cin.tie(0);
using namespace std;

int main(){
	weakson;
	int n;
	cin >> n;
	vector<ll> v(n);
	for (int i = 0; i < n; i++) cin >> v[i];
	vector<ll> dp(n);
	for (int i = 0; i < n; i++){
	}
}
