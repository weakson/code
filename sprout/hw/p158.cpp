#include<bits/stdc++.h>
#define ll long long
#define weakson ios::sync_with_stdio(0), cin.tie(0);
using namespace std;

int main(){
	weakson;
	int t;
	cin >> t;
	while (t--){
		int n, m, k;
		cin >> n >> m >> k;

		vector<ll> a(n), b(n);

		for (int i = 0; i < n; i++) cin >> a[i] >> b[i];

		for (int i = 0; i < n; i++){
			for (int j = m; j >= a[i]
		}
	}
}
